# Project Instructions

- Do not start the simulator dev server, and do not request permission to run `npm run dev`, unless the user explicitly asks for the dev server to be started.
- For frontend changes, `npm run build` is enough verification by default unless the user asks for browser testing.
